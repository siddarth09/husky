To run:

1. git clone the package into a workspace
2. colcon build, source the package
3. in the workspace/src folder, run the command below
4. gz sim husky/model/husky.sdf --verbose --physics-engine gz-physics-bullet-featherstone-plugin 
